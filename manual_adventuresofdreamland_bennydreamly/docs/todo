# TODO add 2x progressive command to starting items when unstable build becomes stable
# TODO default new options to false, instead of defaulting to true
# TODO fix solo generations with roomsanity + additional options, as it currently does not generate